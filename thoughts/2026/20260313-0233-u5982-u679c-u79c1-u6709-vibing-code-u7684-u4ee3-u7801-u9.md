---
date: "2026-03-13T02:33:18.787477455+00:00"
---
如果 vibing code 的代码默认假设就不会被人类阅读，那每个文件不超过几百行的约束是不是已经没有意义了。也许可以试一下从 mono repo 切换到 mono file。

但直接一步到位省掉编译器环节，直接让模型生成汇编甚至二进制，似乎又过于激进了点。也许需要的是中间再加一层适配模型的转换。「任何问题都可以通过往中间加一层来解决」。
